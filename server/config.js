const config = {
    serve: {
        host: 'localhost',
        port: 8080
    },
    mongo: {
            host: 'localhost',
            port: '27017',
            name: 'wordsGameDB'
    }
};

module.exports = config;
